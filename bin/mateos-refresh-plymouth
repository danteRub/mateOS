#!/bin/bash

sudo cp ~/.local/share/mateos/default/plymouth/* /usr/share/plymouth/themes/mateos/
sudo plymouth-set-default-theme mateos

if command -v limine-mkinitcpio &>/dev/null; then
  sudo limine-mkinitcpio
else
  sudo mkinitcpio -P
fi
